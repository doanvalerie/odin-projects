<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0" />
  </head>
  <body>
    <h1><%= title %></h1>
    <button onclick="location.href='/new'">
      <span class="material-symbols-outlined">
        sticky_note_2
      </span>
      CREATE POST
    </button>
    <div id="grid">
      <% for (let i = messages.length - 1; i >= 0; i--) { %>
        <div class="note" style="background-color: <%= messages[i].color %>">
          <div class="note_meta">
            <p class="note_title"><%= messages[i].title %></p>
            <p>User: <%= messages[i].user %></p>
            <p>Posted: <%= messages[i].added %></p>
          </div>
          <p><%= messages[i].text %></p>
        </div>
      <% } %>
    </div>
  </body>
</html>